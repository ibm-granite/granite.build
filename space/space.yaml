name: standalone
secret_manager:
  type: env
  config: {}
base_uris:
  - file://.
  - file://../assets
  - file://../src/gbserver/builtins
variables:
  DEFAULT_ENVIRONMENT: skypilot
  S3_DIGIT_INPUT_URI: "s3://<your-bucket>/digit/input"
  S3_DIGIT_OUTPUT_URI: "s3://<your-bucket>/digit/output"
  S3_SAGE_OUTPUT_URI: "s3://<your-bucket>/sage/output"
