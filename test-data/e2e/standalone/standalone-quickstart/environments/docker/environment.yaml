name: docker-standalone
type: Docker
config:
  defaults:
    pull_policy: "if-not-present"
    env: {}
assetstores:
  - store_uri: space://assetstores/hf/
    load:
      - mode: default
  # file: URIs are served by the implicitly-registered builtin file store.
