granite.build:
  name: bash-gpu-test
  targets:
    gpu-check:
      environment_uri: space://environments/bash
      inputs: {}
      outputs: {}
      steps:
        - step_uri: space://steps/gpu-check
          config:
            compute_config:
              num_nodes: 1
