# Fixture for test_cancel_reaps_workload_child in
# test/integration/standalone/buildrunner/bash/test_buildrunner_retry_cancellation.py.
#
# Points at build-orphan.yaml, whose workload sleeps 600s with a unique sentinel
# token so the test can assert (via psutil) that cancelling the build reaps the
# workload process group and leaves no orphan.
build_yaml: build-orphan.yaml
expected_status: cancelled
simulate_step_failure: false
space_uri: ../../../../../../configurations/spaces/local
target_expectations: []
timeout_minutes: 10
