# gres.conf -- SLURM Generic Resource (GRES) configuration
#
# Empty for now -- no GPU resources in the Docker development cluster.
# GPU GRES can be added when containers have GPU device passthrough.
