# gres.conf -- SLURM Generic Resource (GRES) configuration
#
# Defines 1 virtual GPU per compute node for Docker development cluster.
# No physical GPU hardware required.

AutoDetect=off
Name=gpu Count=1
