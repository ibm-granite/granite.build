# gres.conf -- SLURM Generic Resource (GRES) configuration
#
# c1 has 1 GPU via nvidia-container-toolkit passthrough from host.
# AutoDetect=nvidia lets SLURM discover the GPU via the nvidia plugin.

AutoDetect=nvidia
